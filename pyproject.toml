[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agentmodel"
version = "0.2.0"
description = "Agent's data model"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
]
