[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agentmodel"
version = "1.0.40"
description = "Agent's data model"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
]
